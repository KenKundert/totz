date_fmt = 'YYYY-MM-DD'
major = 1
minor = 3
patch = 1
date = ['README.rst Released']
version = [
    'setup.py version',
    'README.rst Version',
]
status_command = 'hg status -q'
commit_command = "hg commit -m 'bump version to {version}'"
tag_command = "git tag -s v{version} -m 'bump version to {version}'"
